<% if @current_user.liked_by?(post.id) %>
  <%= link_to '☆', destroy_like_path(post), method: :DELETE, remote: true %><%= post.likes.count %>
<% else %>
  <%= link_to '★', create_like_path(post), method: :POST, remote: true %><%= post.likes.count %>
<% end %>
