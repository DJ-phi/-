<% if @current_user.post_liked_by?(post.id) %>
  <%= link_to '★', destroy_post_like_path(post), method: :DELETE, remote: true %><%= post.post_likes.size %>
<% else %>
  <%= link_to '☆', create_post_like_path(post), method: :POST, remote: true %><%= post.post_likes.size %>
<% end %>
