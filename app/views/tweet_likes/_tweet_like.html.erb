<% if @current_user.tweet_liked_by?(tweet.id) %>
  <%= link_to '❤︎', destroy_tweet_like_path(tweet), method: :DELETE, remote: true %><%= tweet.tweet_likes.size %>
<% else %>
  <%= link_to '♡', create_tweet_like_path(tweet), method: :POST, remote: true %><%= tweet.tweet_likes.size %>
<% end %>
